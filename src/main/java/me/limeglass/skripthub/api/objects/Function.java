package me.limeglass.skripthub.api.objects;

public interface Function extends Syntax {
	//Empty because there is no documentation to grab from.
	//May be useful for someone and for future implementation.
}
